syntax = "proto3";

import "backend/v1/user_summary.proto";

package backend.v1;

// Detailed user information. Used to render the Profile page.
message UserDetails {
  UserSummary summary = 1;
  optional string bio = 2;
}